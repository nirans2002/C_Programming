#include<stdio.h>
#include<conio.h>
#include<string.h>

void main()
{
    char str[];
    int i,j,n;
    printf("Enter the string to check:\n");
    gets(str);
    n=strlen(str);
    // check
    while (str[i]!='\0')
    {
        if (str[j] == str[i])
        {
            
        } 
    }
}